/** Dropdown portal helper: keeps menus above clipping containers. */

export function installDropdownPortal() {
  // For now we rely on CSS overflow fixes. This hook stays as a future expansion
  // point if we need to reparent dropdown lists to body.
  // No-op to keep behaviour stable.
}

