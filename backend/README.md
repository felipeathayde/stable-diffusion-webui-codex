DEPRECATED BACKEND PACKAGE
==========================

This package is no longer used. Active server/backend code lives under:

  apps/server/backend

Do not import `backend.*`. Use the façade `apps.server.backend.*` instead.

