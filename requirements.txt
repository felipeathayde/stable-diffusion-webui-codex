# Codex WebUI (backend) Python requirements (legacy; pinned, torch excluded)
#
# Important:
# - This repo now uses `uv.lock` + `pyproject.toml` as the source of truth for dependency versions.
# - This file is kept for reference only and is no longer used by the installers.
# - Keep "extra" research/devenv packages (e.g. pyiqa/datasets/numba/opencv) in a separate venv.
#
# This file is meant to be a "known good" baseline for community installs.

# API server
fastapi==0.118.3
starlette==0.48.0
uvicorn==0.37.0
pydantic==2.11.10
httpx==0.28.1
python-multipart==0.0.20

# Core utilities
requests==2.32.5
rich==14.2.0
tqdm==4.67.1
psutil==7.1.0
PyYAML==6.0.2
piexif==1.1.3

# Model stack (Hugging Face / Diffusers)
accelerate==1.10.1
diffusers==0.36.0
transformers==4.57.3
peft==0.17.1
huggingface-hub==0.36.0
tokenizers==0.22.1
safetensors==0.7.0
einops==0.8.1
sentencepiece==0.2.1

# Imaging / numerics
numpy==1.26.4
Pillow==10.4.0

# Small pins to keep resolver stable across platforms
filelock==3.20.0
packaging==25.0
regex==2025.9.18
typing-extensions==4.15.0

# Windows-only niceties (optional in code, but improves console UX)
colorama==0.4.6; platform_system == "Windows"
